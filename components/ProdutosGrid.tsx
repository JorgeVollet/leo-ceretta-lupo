// (Reservado) Grade de produtos item-a-item — planejado para a Área do Cliente (Fase 2/3).
// Não usado na Fase 1. Mantido como placeholder pra evitar import quebrado se referenciado.
export default function ProdutosGrid() {
  return null;
}
